Задача: Отсортировать массив данных

Алгоритм:
1. Если a[i]>a[i+1], то обменять их местами;
2. После первого прохода максимальный элемент встанет на свое место.
3. После второго прохода второй по величине элемент встанет на свое место.
...


Время:
Худший случай: O(N - 2 + N - 3 + N - 4 ... + 3 + 2 + 1) = O((N-1)(N-2)/2) = O(N^2)
Лучший случай: О(N) - массив уже отсортирован, мы пройдемся по всем элементам и поймем, что
ни одного swap'a не было, то есть все элементы уже отсортированы, поэтому закончим алгоритм

Пояснение: для каждого последующего элемента будет выполнено на одну проверку меньше, в итоге
получаем N^2

Память: М(1) - никаких новых переменных не создаем

Примечание:
Алгоритм считается учебным и практически не применяется вне учебной литературы,
вместо него на практике применяются более эффективные алгоритмы сортировки.
В то же время метод сортировки обменами лежит в основе некоторых более совершенных алгоритмов,
таких как шейкерная сортировка, пирамидальная сортировка и быстрая сортировка.

