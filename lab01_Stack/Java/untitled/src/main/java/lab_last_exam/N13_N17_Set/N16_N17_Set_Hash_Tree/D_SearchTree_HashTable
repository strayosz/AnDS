Вместо списков в используем деревья поисков, тогда:
Время: О(1) ... O(logN)
в среднем О(1)
Память: М(n)

Но: у дерева константа все же больше из-за балансировок и подобных операций

Дополнительные возможности:
минимальный элемент;  min = st.first();
максимальный элемент;  max = st.last();
следующий по порядку элемент;  next = st.higher(t);
