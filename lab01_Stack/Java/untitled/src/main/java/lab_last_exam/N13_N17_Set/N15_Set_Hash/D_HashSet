Что такое хеш-функция
Определение
Хэш-функция - функция, позволяющая получить для определенного значения key, единственно
 возможное значение hash. Для одного key может быть ровно одно значение hash.
 Для одного hash, могут быть разные key.

Определение
Коллизия - это ситуация, когда разным key соответствует одно значение hash.
Разрешение коллизий - программное решение, позволяющее хранить разные объекты с
одинаковым значением hash. То есть не терять

Хорошая и идеальная хеш-функции
Хорошей хеш-функцией считается та, которая допускает наименьшее количество коллизий,
при этом не занимает много много времени для вычисления.
Для хорошей хеш-функции m>n, то есть различных результатов хеш-функции должно
быть больше ожидаемого количества элементов в хранилище.
Идеальной хеш-функцией (англ. perfect hash function) называется такая функция,
которая отображает каждый ключ из набора S во множество целых чисел без коллизий.
В математике такое преобразование называется инъективным отображением.

HashSet - это реализация интерфейса Set через использование хеш-функции.
Примитивная реализация каждого из основных методов HashSet:
1. добавление элемента:
if (!a[h(x)]) s++;
a[h(x)] = true;

2. проверить, есть ли элемент:
a[h(x)];

3. удалить элемент:
if (a[h(x)]) s--;
a[h(x)] = false;

4. размер:
return s;

HashSet основан на хэш-таблице, и операции добавления, удаления или поиска
в среднем будут выполняться за константное О(1) время.
в худшем случае за O(n)
По памяти М(n + m), где m – размер списка, а n – количество элементов