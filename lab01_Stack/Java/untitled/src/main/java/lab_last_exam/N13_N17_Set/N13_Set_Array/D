Set - это множество или мешок.

Множество - это такой же способ хранения данных, как массив или список. Но особенность множества в том, что оно может хранить только уникальные значения.

Возможные операции:
добавить элемент;  s.add(x);
проверить, есть ли элемент  s.contains(x);
удалить элемент   s.remove(x);
размер s.size();

Время: O(N), кроме size(), он O(1)

Память: M(C) - зависит от реализации

Примечания:
применимо всегда, но большая сложность по времени